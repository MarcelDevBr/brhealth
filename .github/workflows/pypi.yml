# Copyright (c) 2024-2026 Marcel <MarcelDevBr> and BRHealth Contributors.
# Licensed under the GNU Affero General Public License v3 (AGPLv3)
# or a commercial license agreement directly with the author.

name: Build & Publish Python Wheels (PyPI & GitHub)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Tag version to build wheels for (e.g. v0.1.0)"
        required: true
        default: "v0.1.0"

permissions:
  contents: write
  id-token: write # Necessário para autenticação OIDC Trusted Publishing no PyPI

jobs:
  build-wheels:
    name: Build Wheels (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64 (glibc / manylinux)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            sdist: true

          # Linux ARM64 (aarch64)
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            sdist: false

          # macOS Apple Silicon (ARM64)
          - os: macos-latest
            target: aarch64-apple-darwin
            sdist: false

          # macOS Intel (x86_64)
          - os: macos-13
            target: x86_64-apple-darwin
            sdist: false

          # Windows x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            sdist: false

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Build Wheels via Maturin
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --manifest-path crates/brhealth-python/Cargo.toml --out dist
          sdist: ${{ matrix.sdist }}

      - name: Upload Wheel Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.target }}
          path: dist/*

  publish:
    name: Publish Wheels to GitHub Releases & PyPI
    needs: build-wheels
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download All Wheel Artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true

      - name: Display Generated Wheels
        shell: bash
        run: |
          echo "=== Pacotes Python (.whl / .tar.gz) Gerados ==="
          ls -lh dist/

      - name: Attach Wheels to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag_name || (github.ref_type == 'tag' && github.ref_name) || 'v0.1.0' }}
          files: dist/*

      - name: Publish to PyPI (Trusted Publishing)
        uses: pypa/gh-action-pypi-publish@release/v1
        continue-on-error: true
        with:
          packages-dir: dist/
